// Generated by jextract

package io.github.chrisribble.ffmpeg7;

import java.lang.invoke.*;
import java.lang.foreign.*;
import java.nio.ByteOrder;
import java.util.*;
import java.util.function.*;
import java.util.stream.*;

import static java.lang.foreign.ValueLayout.*;
import static java.lang.foreign.MemoryLayout.PathElement.*;

/**
 * {@snippet lang=c :
 * typedef int (*av_format_control_message)(struct AVFormatContext {
 *     const AVClass *av_class;
 *     const struct AVInputFormat *iformat;
 *     const struct AVOutputFormat *oformat;
 *     void *priv_data;
 *     AVIOContext *pb;
 *     int ctx_flags;
 *     unsigned int nb_streams;
 *     AVStream **streams;
 *     unsigned int nb_stream_groups;
 *     AVStreamGroup **stream_groups;
 *     unsigned int nb_chapters;
 *     AVChapter **chapters;
 *     char *url;
 *     int64_t start_time;
 *     int64_t duration;
 *     int64_t bit_rate;
 *     unsigned int packet_size;
 *     int max_delay;
 *     int flags;
 *     int64_t probesize;
 *     int64_t max_analyze_duration;
 *     const uint8_t *key;
 *     int keylen;
 *     unsigned int nb_programs;
 *     AVProgram **programs;
 *     enum AVCodecID video_codec_id;
 *     enum AVCodecID audio_codec_id;
 *     enum AVCodecID subtitle_codec_id;
 *     enum AVCodecID data_codec_id;
 *     AVDictionary *metadata;
 *     int64_t start_time_realtime;
 *     int fps_probe_size;
 *     int error_recognition;
 *     AVIOInterruptCB interrupt_callback;
 *     int debug;
 *     int max_streams;
 *     unsigned int max_index_size;
 *     unsigned int max_picture_buffer;
 *     int64_t max_interleave_delta;
 *     int max_ts_probe;
 *     int max_chunk_duration;
 *     int max_chunk_size;
 *     int max_probe_packets;
 *     int strict_std_compliance;
 *     int event_flags;
 *     int avoid_negative_ts;
 *     int audio_preload;
 *     int use_wallclock_as_timestamps;
 *     int skip_estimate_duration_from_pts;
 *     int avio_flags;
 *     enum AVDurationEstimationMethod duration_estimation_method;
 *     int64_t skip_initial_bytes;
 *     unsigned int correct_ts_overflow;
 *     int seek2any;
 *     int flush_packets;
 *     int probe_score;
 *     int format_probesize;
 *     char *codec_whitelist;
 *     char *format_whitelist;
 *     char *protocol_whitelist;
 *     char *protocol_blacklist;
 *     int io_repositioned;
 *     const struct AVCodec *video_codec;
 *     const struct AVCodec *audio_codec;
 *     const struct AVCodec *subtitle_codec;
 *     const struct AVCodec *data_codec;
 *     int metadata_header_padding;
 *     void *opaque;
 *     av_format_control_message control_message_cb;
 *     int64_t output_ts_offset;
 *     uint8_t *dump_separator;
 *     int (*io_open)(struct AVFormatContext *, AVIOContext **, const char *, int, AVDictionary **);
 *     int (*io_close2)(struct AVFormatContext *, AVIOContext *);
 * } *, int, void *, size_t)
 * }
 */
public class av_format_control_message {

    av_format_control_message() {
        // Should not be called directly
    }

    /**
     * The function pointer signature, expressed as a functional interface
     */
    public interface Function {
        int apply(MemorySegment s, int type, MemorySegment data, long data_size);
    }

    private static final FunctionDescriptor $DESC = FunctionDescriptor.of(
        FFmpeg.C_INT,
        FFmpeg.C_POINTER,
        FFmpeg.C_INT,
        FFmpeg.C_POINTER,
        FFmpeg.C_LONG
    );

    /**
     * The descriptor of this function pointer
     */
    public static FunctionDescriptor descriptor() {
        return $DESC;
    }

    private static final MethodHandle UP$MH = FFmpeg.upcallHandle(av_format_control_message.Function.class, "apply", $DESC);

    /**
     * Allocates a new upcall stub, whose implementation is defined by {@code fi}.
     * The lifetime of the returned segment is managed by {@code arena}
     */
    public static MemorySegment allocate(av_format_control_message.Function fi, Arena arena) {
        return Linker.nativeLinker().upcallStub(UP$MH.bindTo(fi), $DESC, arena);
    }

    private static final MethodHandle DOWN$MH = Linker.nativeLinker().downcallHandle($DESC);

    /**
     * Invoke the upcall stub {@code funcPtr}, with given parameters
     */
    public static int invoke(MemorySegment funcPtr,MemorySegment s, int type, MemorySegment data, long data_size) {
        try {
            return (int) DOWN$MH.invokeExact(funcPtr, s, type, data, data_size);
        } catch (Throwable ex$) {
            throw new AssertionError("should not reach here", ex$);
        }
    }
}

